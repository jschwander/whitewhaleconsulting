/** Canonical production host (include www). */
export const CANONICAL_HOST = 'www.whitewhaleconsulting.com';

/** Apex / naked domain — only used as a redirect source, never a destination. */
export const APEX_HOST = 'whitewhaleconsulting.com';

export const CANONICAL_SITE_URL = `https://${CANONICAL_HOST}`;
